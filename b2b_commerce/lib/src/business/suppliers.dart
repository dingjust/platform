import 'package:flutter/material.dart';

class SuppliersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('供应商管理'),
      ),
      body: Text('TODO: 供应商管理'),
    );
  }
}
