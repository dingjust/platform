import 'package:flutter/material.dart';

class RequirementOrdersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('需求订单'),
      ),
      body: Text('TODO: 需求订单'),
    );
  }
}
