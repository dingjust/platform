import 'package:flutter/material.dart';

class ProductionOrdersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('生产订单'),
      ),
      body: Text('TODO: 生产订单'),
    );
  }
}
