import 'package:flutter/material.dart';

class SampleProductBorrowedHistoryPage extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text('借入样衣'),
      ),
      body: Text(''),
    );
  }
}