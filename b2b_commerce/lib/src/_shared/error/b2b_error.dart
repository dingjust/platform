import 'package:flutter/material.dart';

///错误组件显示
class B2BErrorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        '',
        textAlign: TextAlign.center,
      ),
    );
  }
}
